// double as the data type used to store decimal value,in the below example data used to store the simplest value which is decimal
void main() {
  double p = 50.6;
  double r = 2.9;
  double t = 45;

  double I = p * r * t / 100;
  print("simple interest(I) is $I");
}
